<h2>New ticket</h2>

<%= render "form.html", changeset: @changeset,
                        action: ticket_path(@conn, :create), users: @users, tags: @tags %>

<%= link "Back", to: ticket_path(@conn, :index) %>
