<h2>Edit ticket</h2>

<%= render "form.html", changeset: @changeset,
                        action: ticket_path(@conn, :update, @ticket), users: @users, tags: @tags%>

<%= link "Back", to: ticket_path(@conn, :index) %>
