<h2>Please create a company!</h2>

<%= render "form.html", changeset: @changeset,
                        action: company_path(@conn, :create) %>

<%= link "Join a company", to: company_path(@conn, :join) %>
